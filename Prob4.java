//There are two sorted arrays nums1 and nums2 of size m and n respectively. \
//Find the median of the two sorted arrays.
//The overall run time complexity should be O(log (m+n)).

// Using binary search: general goal, find divide position to make the number in left side = right side
public class Prob4 {
	
	static public int findMedian(int[] A, int[] B) {
		int rangeA = A.length, rangeB = B.length;
		// Break point goes from the left and right respectively,
		
		return 0;
	}
	
	static void main(String[] args) {
		int[] A = {1, 2, 3, 4, 9};
		int[] B = {3, 7};
		findMedian(A, B);
	}
}
